#include <stdio.h>
#include<string.h>
void main()
{
  char password[100];
  char ch;
  int i=0;
  printf("Enter any password");
  while((ch=getch())!=13)
  {
      password[i]=ch;
      i=i+1;
      printf(" * ");
  }
  password[i]='\0';
  printf("Entered password is %s",password);
}
