#include <stdio.h>
#include<string.h>
void main()
{
  char para[100];
  printf("Enter a paragraph");
  scanf("%s",para);
  int c=0;
  for(int i=0;i<strlen(para);i++)
  {
    if(para[i]==' ' && para[i+1]!=' ')
    c=c+1;
      printf("%d",c+1);
  }

}
